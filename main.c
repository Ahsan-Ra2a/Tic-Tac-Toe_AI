#include "tic-tac-toe.h"
#include <stdio.h>

int main(void)
{
    char board[BOARD_SIZE][BOARD_SIZE] = {
        {'.', '.', '.'},
        {'.', '.', '.'},
        {'.', '.', '.'}
    };

    int choice;

    do 
    {

    } while (choice != 0);
}